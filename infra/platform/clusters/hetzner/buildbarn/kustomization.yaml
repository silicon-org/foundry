apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Storage comes from the base unchanged: this cluster has a StorageClass, so the
# PersistentVolumeClaim is honoured and the cache survives a restart -- and,
# because the class reclaims rather than deletes, survives the cluster too. That
# is the point of running one at all.
resources:
  - ../../../buildbarn
  - storage-volume.yaml
patches:
  # The nodes here are x86, and a worker that advertised the wrong architecture
  # would be given actions it cannot run. See //platforms for the other end of
  # this agreement.
  - target:
      kind: Deployment
      name: worker
    patch: |
      - op: replace
        path: /spec/template/spec/containers/0/env/2/value
        value: amd64

  # ccx33 is 8 dedicated cores, so one action at a time would waste most of the
  # machine. Six rather than eight leaves headroom: the worker container also
  # materialises inputs and uploads outputs, and a node saturated by its own
  # build actions starves its kubelet -- which is exactly how the local cluster
  # fell over, and it took a long time to recognise because the symptom was DNS.
  - target:
      kind: Deployment
      name: worker
    patch: |
      - op: replace
        path: /spec/template/spec/containers/0/env/3/value
        value: "6"

  # Limits sized to the node rather than to a laptop. Six concurrent
  # compilations need real memory, and the limits are what stop a runaway build
  # taking the control plane with it.
  - target:
      kind: Deployment
      name: worker
    patch: |
      - op: replace
        path: /spec/template/spec/containers/0/resources/requests/cpu
        value: "1"
      - op: replace
        path: /spec/template/spec/containers/0/resources/limits/memory
        value: 4Gi
      - op: replace
        path: /spec/template/spec/containers/1/resources/requests/cpu
        value: "4"
      - op: replace
        path: /spec/template/spec/containers/1/resources/limits/memory
        value: 20Gi
