"""Generates a CHI mesh: the RTL, the package that describes it, and the JSON a
testbench checks it against.

See //hardware/ip/chi_noc/README.md for the architecture, and `config.Topology`
for what a description may say.
"""
