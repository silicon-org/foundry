apiVersion: v1
kind: Namespace
metadata:
  name: bb-portal
  labels:
    # Same baseline as everything else that does not need privilege.
    pod-security.kubernetes.io/enforce: restricted
    pod-security.kubernetes.io/enforce-version: latest
